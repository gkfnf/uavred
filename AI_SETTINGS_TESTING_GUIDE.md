# AI 供应商设置界面测试完整指南

## 概述

本指南帮助你验证 AI 供应商设置界面的所有交互功能是否正常工作。

## 快速开始

### 1. 启动应用

```bash
cd /Users/fk/Devlopment/uavred
cargo run
```

### 2. 进入 AI 设置

1. 点击顶部导航栏的 "Settings"
2. 在左侧边栏点击 "AI"

---

## 功能测试清单

### ✅ 测试 1: 供应商列表显示

**目的**: 验证所有供应商正确显示

**步骤**:
1. 进入 AI 设置页面
2. 查看左侧供应商列表

**预期结果**:
- [ ] 显示 5 个供应商：
  - Kimi (Moonshot)
  - DeepSeek
  - OpenAI
  - Claude
  - Ollama
- [ ] 每个供应商显示图标
- [ ] 每个供应商显示名称和描述
- [ ] 每个供应商显示启用模型数量徽章
- [ ] 每个供应商显示状态指示灯

### ✅ 测试 2: 供应商选择

**目的**: 验证点击供应商切换配置面板

**步骤**:
1. 点击 "DeepSeek" 供应商卡片
2. 查看右侧配置面板
3. 点击 "Kimi" 供应商卡片
4. 查看右侧配置面板

**预期结果**:
- [ ] 点击 DeepSeek 后，右侧面板显示 DeepSeek 配置
- [ ] 点击 Kimi 后，右侧面板显示 Kimi 配置
- [ ] 选中的供应商卡片高亮显示（灰色背景）
- [ ] 切换时无卡顿或闪烁

### ✅ 测试 3: 启用/禁用供应商

**目的**: 验证供应商开关功能

**步骤**:
1. 选择 Kimi 供应商
2. 点击配置面板顶部的启用开关
3. 观察状态指示灯
4. 再次点击开关

**预期结果**:
- [ ] 开关可以正常切换
- [ ] 启用后，供应商卡片上的指示灯变绿色
- [ ] 禁用后，指示灯变灰色
- [ ] 切换时设置自动保存

### ✅ 测试 4: API 配置输入

**目的**: 验证端点和 API Key 输入

**步骤**:
1. 选择 DeepSeek 供应商
2. 在 "API Endpoint" 输入框输入 `https://custom.deepseek.com`
3. 在 "API Key" 输入框输入 `sk-test123`
4. 点击其他区域或切换到其他供应商再切回来

**预期结果**:
- [ ] 端点输入框接受文本输入
- [ ] API Key 输入框接受文本输入（显示为密码样式）
- [ ] 输入的值被保留
- [ ] 默认值根据供应商自动填充

### ✅ 测试 5: 供应商搜索过滤

**目的**: 验证供应商搜索功能

**步骤**:
1. 在供应商列表顶部的搜索框输入 `deep`
2. 查看列表结果
3. 清空搜索框

**预期结果**:
- [ ] 输入 `deep` 后，只显示 DeepSeek
- [ ] 输入 `kimi` 后，只显示 Kimi
- [ ] 输入 `ai` 后，显示多个匹配的供应商
- [ ] 清空搜索框后，显示所有供应商

### ✅ 测试 6: 连接测试

**目的**: 验证 Test Connection 功能

**步骤**:
1. 选择 DeepSeek 供应商
2. 输入有效的 API Key（如果有）或无效的值 `invalid-key`
3. 点击 "Test Connection" 按钮

**预期结果**:
- [ ] 点击后按钮显示加载状态
- [ ] 无效 Key：底部显示红色错误消息
- [ ] 有效 Key：底部显示绿色成功消息，包含延迟时间

### ✅ 测试 7: 获取模型

**目的**: 验证 Fetch Models 功能

**步骤**:
1. 选择 Kimi 供应商
2. 输入有效的 API Key
3. 点击 "Fetch Models" 按钮

**预期结果**:
- [ ] 点击后按钮显示加载状态
- [ ] 成功获取后，模型列表更新
- [ ] 底部显示绿色成功消息，包含模型数量
- [ ] 失败时显示红色错误消息

### ✅ 测试 8: 模型启用/禁用

**目的**: 验证模型开关功能

**步骤**:
1. 选择有模型的供应商（如已获取模型的 Kimi）
2. 查看模型列表
3. 点击某个模型的开关
4. 查看供应商卡片上的启用数量徽章

**预期结果**:
- [ ] 模型开关可以正常切换
- [ ] 启用/禁用模型后，供应商卡片的徽章数量更新
- [ ] 设置自动保存

### ✅ 测试 9: 模型搜索过滤

**目的**: 验证模型搜索功能

**步骤**:
1. 选择有多个模型的供应商
2. 在模型搜索框输入 `k2`
3. 查看过滤结果

**预期结果**:
- [ ] 搜索过滤模型列表
- [ ] 搜索匹配模型 ID 和名称
- [ ] 清空搜索框显示所有模型

### ✅ 测试 10: 集成配置显示

**目的**: 验证集成配置区域

**步骤**:
1. 选择 Kimi 供应商
2. 查看配置面板下方的集成区域
3. 选择 DeepSeek 供应商
4. 查看集成区域
5. 选择 OpenAI 供应商

**预期结果**:
- [ ] Kimi 显示 "Kimi Code Integration" 区域，包含环境变量配置代码块
- [ ] DeepSeek 显示 "DeepSeek Integration" 区域
- [ ] OpenAI 不显示集成区域（尚未实现）

### ✅ 测试 11: 保存设置

**目的**: 验证 Save Settings 功能

**步骤**:
1. 修改任意配置（如启用/禁用供应商）
2. 点击 "Save Settings" 按钮

**预期结果**:
- [ ] 点击后显示加载状态
- [ ] 底部显示绿色成功消息 "Settings saved"
- [ ] 配置持久化到文件

### ✅ 测试 12: 状态消息显示

**目的**: 验证操作反馈消息

**步骤**:
1. 执行各种操作（保存、测试连接、获取模型等）
2. 观察底部状态消息

**预期结果**:
- [ ] 成功操作显示绿色背景消息
- [ ] 失败操作显示红色背景消息
- [ ] 消息内容清晰明了

---

## 使用 Testsprite 进行自动化测试

由于 UAVRed 是原生 GPUI 应用，无法直接使用浏览器自动化测试。但你可以使用以下方法：

### 方法 1: 手动截图对比

1. 使用 `testsprite_bootstrap` 启动应用（如果支持原生应用测试）
2. 导航到 AI 设置页面
3. 截取关键状态的屏幕截图
4. 对比预期和实际的 UI

### 方法 2: 创建测试计划并手动执行

我已经为你创建了测试计划文件：

```bash
# 查看测试计划
cat /Users/fk/Devlopment/uavred/testsprite_tests/frontend_test_plan.json
```

### 方法 3: GPUI 单元测试

由于 GPUI 测试环境限制，单元测试需要在支持图形界面的环境中运行：

```bash
# 在支持图形的 macOS 环境中运行
cargo test -p settings_ui
```

---

## 调试技巧

### 查看日志输出

```bash
# 启用详细日志
RUST_LOG=debug cargo run

# 过滤 settings_ui 相关日志
RUST_LOG=settings_ui=debug cargo run
```

### 检查配置文件

设置保存在：

- **macOS**: `~/Library/Application Support/uavred/settings.json`
- **Linux**: `~/.config/uavred/settings.json`

查看文件内容验证设置是否正确保存：

```bash
cat ~/Library/Application\ Support/uavred/settings.json
```

### 手动检查代码逻辑

关键文件位置：

| 功能 | 文件路径 |
|------|----------|
| AI 设置面板 | `crates/settings_ui/src/ai_settings.rs` |
| 配置管理 | `crates/settings_ui/src/config.rs` |
| 供应商实现 | `crates/settings_ui/src/provider/` |
| UI 组件 | `crates/settings_ui/src/components/` |

---

## 常见问题

### Q: 为什么供应商列表为空？

**可能原因**:
1. 配置文件损坏 - 尝试删除配置文件后重启
2. 初始化错误 - 查看日志检查错误信息

### Q: 连接测试总是失败？

**排查步骤**:
1. 检查网络连接
2. 验证 API Key 是否正确
3. 检查端点 URL 格式
4. 查看日志获取详细错误

### Q: 设置没有保存？

**排查步骤**:
1. 点击 "Save Settings" 按钮
2. 查看底部状态消息
3. 检查配置文件权限
4. 查看日志中的保存错误

---

## 代码审查清单

如果你需要审查 AI 设置相关的代码，请检查：

### ai_settings.rs

- [ ] `select_provider` 正确更新选中状态和输入框值
- [ ] `toggle_provider_enabled` 正确处理启用/禁用逻辑
- [ ] `toggle_model_enabled` 正确切换模型状态
- [ ] `test_connection` 正确处理异步连接测试
- [ ] `fetch_models` 正确处理异步模型获取
- [ ] `save_settings` 正确保存到文件
- [ ] `filtered_providers` 正确实现搜索过滤
- [ ] `filtered_models` 正确实现模型搜索过滤
- [ ] `Render` 实现正确渲染所有 UI 元素

### 错误处理

- [ ] 所有异步操作都有错误处理
- [ ] 用户操作有适当的反馈（成功/失败消息）
- [ ] 没有使用 `unwrap()` 或 `expect()` 在关键路径上

---

## 总结

通过以上测试清单，你可以全面验证 AI 供应商设置界面的交互功能。建议按以下顺序测试：

1. **基础显示** → 测试 1-2
2. **基础交互** → 测试 3-5
3. **功能验证** → 测试 6-8
4. **高级功能** → 测试 9-12

完成所有测试后，界面功能应该已得到充分验证。
