# 添加任务对话框测试指南

## 测试步骤

### 1. 打开对话框
- 在 Mission Control 视图中，点击任何列标题旁的"+"按钮
- 应该看到一个标准对话框，背景有淡色叠加层

### 2. 测试文本输入
- **任务标题**：在标题输入框中输入文字，验证能正确输入
- **任务描述**：在描述输入框中输入多行文字，验证能正确换行
- 两个输入框应该都能接收用户输入

### 3. 测试表单验证
- **不填标题直接点创建**：应该看到红色错误提示"任务标题不能为空"
- **填入标题后再点创建**：错误提示应该消失，对话框关闭，任务被创建

### 4. 验证任务创建
- 在对话框中填入：
  - 标题：例如 "实现登录功能"
  - 描述：例如 "使用 JWT 认证"
  - 其他配置保持默认（Medium 优先级，OPENCODE Agent）
- 点击"创建"按钮
- 对话框应该关闭
- 在对应列中应该能看到新创建的任务卡片

### 5. 测试多次创建
- 重新打开对话框（点击不同列的"+"按钮）
- 验证输入框已清空
- 验证错误提示也已清空
- 再次创建任务

### 6. 测试取消操作
- 打开对话框并输入一些内容
- 点击"取消"按钮或按 Escape 键
- 对话框应该关闭，内容不被保存

## 预期行为

| 操作 | 预期结果 |
|------|--------|
| 点击"+" | 对话框打开，背景淡化 |
| 输入标题 | 文字实时显示在输入框 |
| 输入描述 | 文字支持多行 |
| 空标题 + 点创建 | 显示红色错误信息 |
| 有效表单 + 点创建 | 对话框关闭，任务添加到列 |
| 点取消 | 对话框关闭，内容丢弃 |
| 按 Escape | 对话框关闭 |
| 按 Enter | 提交表单（如果标题非空） |

## 调试技巧

### 检查日志
```bash
cargo build --release
# 观察控制台输出
```

### 验证任务数据
- 检查左侧 Mission Control 的各个列
- 确认任务以正确的优先级显示

### 测试不同列
每个列标题旁都有"+"按钮：
- **To Do** - 新任务应该在此列
- **In Progress** - 如果点击此列的"+"按钮，任务应该在此列
- **In Review** - 在 Code Review 列中创建
- **Done** - 在已完成列中创建
- **Cancelled** - 在已取消列中创建

## 已知限制

1. ⏳ **Agent 选择**：目前只显示，不能实时改变
2. ⏳ **优先级按钮**：目前只显示，不能实时改变
3. ⏳ **草稿保存**：未实现
4. ⏳ **Markdown 预览**：未实现

## 故障排除

### 输入框无法输入
- 检查是否聚焦在输入框上
- 尝试点击输入框后再输入
- 检查是否有其他元素抢占焦点

### 错误信息不显示
- 确认标题字段为空
- 检查浏览器开发者工具的控制台是否有错误

### 任务创建后不显示
- 检查是否在正确的列中查找
- 刷新页面看是否持久化
- 检查 Mission Control 标签页是否处于活跃状态

## 性能测试

### 多任务创建
1. 快速创建 10+ 任务
2. 验证 UI 响应性能
3. 检查是否有内存泄漏

### 大文本输入
1. 在描述字段中粘贴大量文本（>10KB）
2. 验证输入框能正确处理

## 边界情况

1. **非常长的标题**（>100 字符）
2. **特殊字符**（emoji、中文、数学符号）
3. **空格处理**（只有空格的标题）
4. **快速连续提交**（快速点击多次创建按钮）
